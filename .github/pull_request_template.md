## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- What was wrong or missing before. -->

## Checklist

- [ ] `npm run check` passes (typecheck + tests)
- [ ] Touched `src/client/`? Then `npm run build` was run and `public/app.js` is committed
- [ ] New behaviour has a test in `test/`
- [ ] No new runtime dependencies (this tool ships with none)
